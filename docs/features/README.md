# docs/features/

Per-feature / per-component documentation.

| Area | What |
|------|------|
| `superpowers/` | command + multi-SDK workstream — `plans/` (export/lookup, Java/TS/Rust/Go/.NET SDKs, disambiguation) + `specs/` (design specs) |
| `zhtw-db/` | database-backed component — architecture, CLI, strategies, per-database notes (mongodb/mysql/postgres/sqlite) |
