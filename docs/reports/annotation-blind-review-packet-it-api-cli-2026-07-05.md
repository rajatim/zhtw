<!-- zhtw:disable -->
# Annotation Blind Review Packet (2026-07-05)

Backlog: `benchmarks/accuracy/annotation-backlog-v1.json`
Backlog name: `annotation-backlog-v1`
Status filter: `needs_blind_review`
Batch filter: `it-api-cli`
Cases: 25

## Reviewer Rules

- Use a reviewer who did not complete the first pass.
- Do not open the backlog JSON, AI draft report, or first-pass expected output while filling this packet.
- Write Taiwan Traditional Chinese expected output from the Simplified input only.
- Keep the input block unchanged.
- Fill reviewer id, blind expected, and notes for each case.
- Return the completed packet to the maintainer for comparison and adjudication.

## Cases

### it-api-cli-0001

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
这个函数会抛出异常并返回错误码。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0002

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
请在配置文件中启用调试模式。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0003

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
服务器证书已经过期，请重新签发。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0004

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
命令行工具会读取环境变量。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0005

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
接口返回的响应头包含缓存策略。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0006

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
请通过参数指定输出目录。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0007

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
日志等级可以在启动时覆盖。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0008

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
客户端会自动刷新访问令牌。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0009

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
后端服务需要验证请求签名。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0010

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
这个字段支持布尔值和字符串。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0011

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
数据库连接池达到上限。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0012

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
部署流水线会先执行单元测试。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0013

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
前端组件会监听窗口大小变化。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0014

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
请把补丁合并到主分支。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0015

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
文件系统权限不足导致写入失败。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0016

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
请上传配置文件并重新启动服务。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0017

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
进程退出前会释放临时文件。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0018

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
请求超时后会重试三次。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0019

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
缓存键包含用户标识和区域代码。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0020

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
这个端点只接受 JSON 请求。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0021

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
调用接口前请先取得访问令牌。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0022

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
程序会在后台监听端口。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0023

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
请检查响应主体中的错误消息。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0024

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
安装依赖库后请重新构建项目。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

### it-api-cli-0025

- Batch: `it-api-cli`
- Domain: `it`
- Source: maintainer-written short cases; license: CC-BY-4.0

Input:

```text
这个命令会递归扫描子目录。
```

Blind reviewer:

```text

```

Blind expected:

```text

```

Reviewer notes:

```text

```

## Coordinator Import Rules

- If blind expected exactly matches the first pass, set `review.blind_reviewer` and `review.status = "approved"`.
- If blind expected differs but is an acceptable Taiwan variant, record it in `review.acceptable` or adjudicate before approval.
- If blind expected conflicts with the first pass, set `review.disagreement = true` and send the case to adjudication.
- Never mark a case `approved` when first reviewer and blind reviewer are the same person.
