<!-- zhtw:disable -->
# Annotation First-Pass AI Draft（2026-07-05）

## 說明

這份文件是 25 筆 `it-api-cli` backlog input 的 AI 草稿審閱，不是人工 first pass。

不可直接把本表內容批次寫入 `review.expected`，也不可把 `expected_source` 標成 `human_first_pass`。人類 reviewer 必須獨立判斷每筆 expected；若採用或修改本表建議，仍需由 reviewer 對最終 expected 負責。

Backlog：`benchmarks/accuracy/annotation-backlog-v1.json`

## AI 草稿建議

| ID | Input | AI draft expected | 審閱重點 |
|----|-------|-------------------|----------|
| `it-api-cli-0001` | 这个函数会抛出异常并返回错误码。 | 這個函式會拋出例外並回傳錯誤碼。 | 程式語境：函数→函式、异常→例外、返回→回傳。 |
| `it-api-cli-0002` | 请在配置文件中启用调试模式。 | 請在設定檔中啟用除錯模式。 | IT 設定語境：配置文件→設定檔、调试→除錯。 |
| `it-api-cli-0003` | 服务器证书已经过期，请重新签发。 | 伺服器憑證已經過期，請重新簽發。 | TLS/憑證語境：证书→憑證。 |
| `it-api-cli-0004` | 命令行工具会读取环境变量。 | 命令列工具會讀取環境變數。 | CLI 語境：命令行→命令列。 |
| `it-api-cli-0005` | 接口返回的响应头包含缓存策略。 | 介面回傳的回應標頭包含快取策略。 | API 語境：接口→介面、返回→回傳、响应头→回應標頭。 |
| `it-api-cli-0006` | 请通过参数指定输出目录。 | 請透過參數指定輸出目錄。 | 介詞語境：通过参数→透過參數，不是通過測試。 |
| `it-api-cli-0007` | 日志等级可以在启动时覆盖。 | 日誌等級可以在啟動時覆寫。 | 設定 override 語境建議用覆寫，需人工確認是否接受覆蓋。 |
| `it-api-cli-0008` | 客户端会自动刷新访问令牌。 | 用戶端會自動更新存取權杖。 | OAuth 語境：客户端→用戶端、访问令牌→存取權杖；刷新是否用更新需人工確認。 |
| `it-api-cli-0009` | 后端服务需要验证请求签名。 | 後端服務需要驗證請求簽章。 | 資安語境：signature 建議簽章，不是一般簽名。 |
| `it-api-cli-0010` | 这个字段支持布尔值和字符串。 | 這個欄位支援布林值和字串。 | 程式型別語境：字段→欄位、支持→支援、字符串→字串。 |
| `it-api-cli-0011` | 数据库连接池达到上限。 | 資料庫連線池達到上限。 | 資料庫語境：连接池→連線池。 |
| `it-api-cli-0012` | 部署流水线会先执行单元测试。 | 部署管線會先執行單元測試。 | CI/CD 語境：流水线→管線。 |
| `it-api-cli-0013` | 前端组件会监听窗口大小变化。 | 前端元件會監聽視窗大小變化。 | 前端語境：组件→元件、窗口→視窗。 |
| `it-api-cli-0014` | 请把补丁合并到主分支。 | 請把修補程式合併到主分支。 | Git/patch 語境：补丁→修補程式。 |
| `it-api-cli-0015` | 文件系统权限不足导致写入失败。 | 檔案系統權限不足導致寫入失敗。 | File system 固定語境：文件系统→檔案系統。 |
| `it-api-cli-0016` | 请上传配置文件并重新启动服务。 | 請上傳設定檔並重新啟動服務。 | 設定檔語境：配置文件→設定檔。 |
| `it-api-cli-0017` | 进程退出前会释放临时文件。 | 行程結束前會釋放暫存檔。 | OS 語境：进程→行程、临时文件→暫存檔；退出可人工確認為結束或退出。 |
| `it-api-cli-0018` | 请求超时后会重试三次。 | 請求逾時後會重試三次。 | 網路/API 語境：超时→逾時。 |
| `it-api-cli-0019` | 缓存键包含用户标识和区域代码。 | 快取鍵包含使用者識別碼和區域代碼。 | 快取語境：缓存键→快取鍵；标识建議識別碼；代码不應轉程式碼。 |
| `it-api-cli-0020` | 这个端点只接受 JSON 请求。 | 這個端點只接受 JSON 請求。 | API endpoint 語境：端点→端點。 |
| `it-api-cli-0021` | 调用接口前请先取得访问令牌。 | 呼叫介面前請先取得存取權杖。 | API/OAuth 語境：调用→呼叫、接口→介面、访问令牌→存取權杖。 |
| `it-api-cli-0022` | 程序会在后台监听端口。 | 程式會在背景監聽連接埠。 | 執行中程式語境：程序→程式、后台→背景、端口→連接埠。 |
| `it-api-cli-0023` | 请检查响应主体中的错误消息。 | 請檢查回應主體中的錯誤訊息。 | API response body 語境：响应主体→回應主體、错误消息→錯誤訊息。 |
| `it-api-cli-0024` | 安装依赖库后请重新构建项目。 | 安裝相依函式庫後請重新建置專案。 | 開發語境：依赖库→相依函式庫、构建→建置、项目→專案。 |
| `it-api-cli-0025` | 这个命令会递归扫描子目录。 | 這個命令會遞迴掃描子目錄。 | CLI/演算法語境：递归→遞迴。 |

## 建議人工審核順序

1. 先審 `candidate_gap`，因為多半是 zhtw 可能還沒有完全覆蓋的 IT 詞。
2. 再審 `over_conversion_guard`，確認是否會保護正確語境而非過度限制。
3. 人類 reviewer 填入 backlog 時，需同時填：
   - `review.expected`
   - `review.expected_source = "human_first_pass"`
   - `review.first_reviewer`
   - `review.status = "needs_blind_review"`
