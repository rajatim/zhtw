repo_kind: software

# These are stable public quality and multi-language SDK contracts used by
# release workflows, mono-version checks, manifests, and downstream users.
root_directory_allowlist:
  - benchmarks
  - sdk
