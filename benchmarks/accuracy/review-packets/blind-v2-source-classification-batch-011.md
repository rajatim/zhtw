<!-- zhtw:disable -->
# Blind-v2 Source Classification 011

Packet: `benchmarks/accuracy/review-packets/blind-v2-source-classification-batch-011.json`
Cases: 100
Seed: `20260719`
Selection: `equal-source-deterministic-sha256-v1`
Selection round: 1

## Rules

- Read only the input and provenance shown in this packet.
- Do not run zhtw, OpenCC, zhconv, Gemini, or another converter.
- Mark `eligible = no` for malformed, unclear, non-Mandarin, or unsuitable text.
- Script: `simplified`, `mixed`, `traditional`, or `uncertain`.
- Domain: `it_api_cli`, `ui_i18n`, `llm_generated`, `formal_news`, `social_daily`, or `high_stakes`.
- Risk: `candidate_gap`, `over_conversion_guard`, or `baseline_guard`.
- Confidence: `high`, `medium`, or `low`; do not guess when context is insufficient.
- This packet is advisory input classification, not expected-output annotation.

## Cases

### vscode-loc-zh-hans-v1/entry-0051ac02d21f3951

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-0051ac02d21f3951`
- Split: `language_pack`

Input:

```text
你将信任 {1} 上“{0}”下的所有存储库和分支。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-0069e37b6f0e20de

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-0069e37b6f0e20de`
- Split: `language_pack`

Input:

```text
智能体会话视图的图标
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-04436e351d686605

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-04436e351d686605`
- Split: `language_pack`

Input:

```text
此 MCP 服务器已禁用。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-09618f3bdd3c063c

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-09618f3bdd3c063c`
- Split: `language_pack`

Input:

```text
重启扩展
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-0cfd4e1544007e6f

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-0cfd4e1544007e6f`
- Split: `language_pack`

Input:

```text
表示底部面板对齐方式设为居中
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-0e09bd6f9c17b08d

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-0e09bd6f9c17b08d`
- Split: `language_pack`

Input:

```text
添加新挂钩...
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-0eb9c25c118ea361

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-0eb9c25c118ea361`
- Split: `language_pack`

Input:

```text
已配置制表符大小
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-185b91993d167e5b

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-185b91993d167e5b`
- Split: `language_pack`

Input:

```text
操作栏中切换的操作项的背景色。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-19cf8a61d70b2fb0

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-19cf8a61d70b2fb0`
- Split: `language_pack`

Input:

```text
控制是否应记录异步词汇切分。仅用于调试。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-1c52c0716654e119

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-1c52c0716654e119`
- Split: `language_pack`

Input:

```text
是否将 {1} 文件中出现的 {0} 替换为“{2}”?
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-248c8ba7416e401a

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-248c8ba7416e401a`
- Split: `language_pack`

Input:

```text
将大写和小写名称混合在一起。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-26688e3e2c17a256

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-26688e3e2c17a256`
- Split: `language_pack`

Input:

```text
此请求包含一个响应 API tool_search_output 项。未捕获到上一个响应续传标记，因此显示的输入可能是完整请求或历史切片请求，而非仅续传增量。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-28115d85b27abca4

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-28115d85b27abca4`
- Split: `language_pack`

Input:

```text
控制搜索结果的最大数目，可将其设置为 “null”(空)，以返回无限结果。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-2e4d6e8f0c8c6125

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-2e4d6e8f0c8c6125`
- Split: `language_pack`

Input:

```text
引擎兼容性。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-30855006f8ec5f0e

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-30855006f8ec5f0e`
- Split: `language_pack`

Input:

```text
删除自动插入的尾随空白符号。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-30c400765bc76010

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-30c400765bc76010`
- Split: `language_pack`

Input:

```text
导出的聊天没有存储库上下文。未检测到 Git 存储库。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-32ab407624a81ed6

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-32ab407624a81ed6`
- Split: `language_pack`

Input:

```text
可访问差异查看器中“插入”的图标。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-3943d6d54a855e67

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-3943d6d54a855e67`
- Split: `language_pack`

Input:

```text
继续(&&C)
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-3a1e175eae85a6ca

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-3a1e175eae85a6ca`
- Split: `language_pack`

Input:

```text
用于格式化 uri 资源标签的规则。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-3bc2558a39853869

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-3bc2558a39853869`
- Split: `language_pack`

Input:

```text
“editorIndentGuide.background” 已弃用。请改用 “editorIndentGuide.background1”。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-3d6cbb5643cc10df

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-3d6cbb5643cc10df`
- Split: `language_pack`

Input:

```text
放弃到剪贴板
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-3da3a66cc6801c1f

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-3da3a66cc6801c1f`
- Split: `language_pack`

Input:

```text
自以下时间已修改的文件:
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-4369b3e0444509a5

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-4369b3e0444509a5`
- Split: `language_pack`

Input:

```text
要从中提取内容的 URL 数组。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-44cd8d8b3e5f81ec

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-44cd8d8b3e5f81ec`
- Split: `language_pack`

Input:

```text
启动终端的目录
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-451643b3ebef0ac0

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-451643b3ebef0ac0`
- Split: `language_pack`

Input:

```text
速览视图编辑器背景色。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-4c58151a363a4042

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-4c58151a363a4042`
- Split: `language_pack`

Input:

```text
管理受信任的域
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-503b40f2b9c3bc66

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-503b40f2b9c3bc66`
- Split: `language_pack`

Input:

```text
关于(&&A)
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-50fe5444ea0eead4

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-50fe5444ea0eead4`
- Split: `language_pack`

Input:

```text
挂钩命令必须至少指定“command”、“windows”、“linux”或“osx”中的一项。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-54162df786ccad7d

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-54162df786ccad7d`
- Split: `language_pack`

Input:

```text
取消固定会话
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-5442b7940224caa1

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-5442b7940224caa1`
- Split: `language_pack`

Input:

```text
需要聊天用户操作
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-54df0e9edf3d5a1f

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-54df0e9edf3d5a1f`
- Split: `language_pack`

Input:

```text
在终端中运行“{0}”
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-57457c86d93f5f43

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-57457c86d93f5f43`
- Split: `language_pack`

Input:

```text
侧边栏中粘滞滚动的背景色。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-59e7eccd502ceb69

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-59e7eccd502ceb69`
- Split: `language_pack`

Input:

```text
MCP 服务器“{0}”需要访问 {1} 帐户“{2}”。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-5afbf4378bd5830a

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-5afbf4378bd5830a`
- Split: `language_pack`

Input:

```text
活动笔记本编辑器为只读模式。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-5b0abfcd5e3e929e

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-5b0abfcd5e3e929e`
- Split: `language_pack`

Input:

```text
规划方法
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-5dcc08b30461b6b4

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-5dcc08b30461b6b4`
- Split: `language_pack`

Input:

```text
当前可见快速输入的类型
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-5f5b0496699c2ff0

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-5f5b0496699c2ff0`
- Split: `language_pack`

Input:

```text
扩展视图中“配置建议的扩展”操作的图标。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-64a03661b97583f0

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-64a03661b97583f0`
- Split: `language_pack`

Input:

```text
将文件夹添加到上一个活动窗口。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-66638959e243f413

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-66638959e243f413`
- Split: `language_pack`

Input:

```text
键入要打开的编辑器名称。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-695847b5a35b3aea

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-695847b5a35b3aea`
- Split: `language_pack`

Input:

```text
没有可用的重构操作
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-6dbf9426a4d30c06

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-6dbf9426a4d30c06`
- Split: `language_pack`

Input:

```text
已追加新消息 - 预期增长
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-6e8b497db0d8de97

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-6e8b497db0d8de97`
- Split: `language_pack`

Input:

```text
这些按钮与文本筛选器协同工作。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-6e911f787c32b03e

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-6e911f787c32b03e`
- Split: `language_pack`

Input:

```text
粘贴{0}
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-6ec38c7aa4ddc709

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-6ec38c7aa4ddc709`
- Split: `language_pack`

Input:

```text
(未知命令)
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-6f1385d789f24d6e

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-6f1385d789f24d6e`
- Split: `language_pack`

Input:

```text
缩略图(&&M)
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-724594337954550d

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-724594337954550d`
- Split: `language_pack`

Input:

```text
聊天编辑器中的内联装订线菜单。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-72ce544d66a729cb

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-72ce544d66a729cb`
- Split: `language_pack`

Input:

```text
测试开始时打开测试结果视图
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-72e568afd72214df

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-72e568afd72214df`
- Split: `language_pack`

Input:

```text
JSON 验证
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-7e36d9bf95e8bed9

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-7e36d9bf95e8bed9`
- Split: `language_pack`

Input:

```text
确定要移除动态身份验证提供程序“{0}”吗?
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-7ed8bbe9a5f3b75f

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-7ed8bbe9a5f3b75f`
- Split: `language_pack`

Input:

```text
已选项在列表或树非活动时的背景颜色。活动的列表或树具有键盘焦点，非活动的没有。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-7f0e900dae3691eb

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-7f0e900dae3691eb`
- Split: `language_pack`

Input:

```text
关于 Web 视图的重要信息:
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-7f8e48ba6d106341

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-7f8e48ba6d106341`
- Split: `language_pack`

Input:

```text
此任务依赖的其他任务。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-856cbd656423cb94

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-856cbd656423cb94`
- Split: `language_pack`

Input:

```text
从历史记录中移除
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-86458014aa22542c

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-86458014aa22542c`
- Split: `language_pack`

Input:

```text
已安装 VS Code 扩展提供的 MCP 服务器。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-8a6cfdaef94da7ac

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-8a6cfdaef94da7ac`
- Split: `language_pack`

Input:

```text
删除沙盒临时目录中的文件
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-8fc02f08634e4b0d

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-8fc02f08634e4b0d`
- Split: `language_pack`

Input:

```text
选择日志文件
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-94381a24276104af

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-94381a24276104af`
- Split: `language_pack`

Input:

```text
终端将被任务重用，按任意键关闭。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-9b6362038d22e2cf

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-9b6362038d22e2cf`
- Split: `language_pack`

Input:

```text
你正在筛选问题。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-9c70830d7d27fdd0

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-9c70830d7d27fdd0`
- Split: `language_pack`

Input:

```text
向上滚动编辑器悬停。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-9ca9abf622010003

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-9ca9abf622010003`
- Split: `language_pack`

Input:

```text
显示文件(统一)
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-9cbbc08caf93b140

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-9cbbc08caf93b140`
- Split: `language_pack`

Input:

```text
无法写入文件。请打开文件以更正错误或警告，然后重试。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-9ea6ebf32e5ff215

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-9ea6ebf32e5ff215`
- Split: `language_pack`

Input:

```text
已应用代码操作
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-9eb53ad2eb50b106

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-9eb53ad2eb50b106`
- Split: `language_pack`

Input:

```text
清除终端
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-a090a75ec1580ca3

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-a090a75ec1580ca3`
- Split: `language_pack`

Input:

```text
已重新连接到正在运行的任务。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-a099104a591d86b5

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-a099104a591d86b5`
- Split: `language_pack`

Input:

```text
{0} (快照)
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-a10503591691a1f3

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-a10503591691a1f3`
- Split: `language_pack`

Input:

```text
启用自动任务 - 请注意，任务将不会在不受信任的工作区中运行。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-a14d33d9d6cf7275

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-a14d33d9d6cf7275`
- Split: `language_pack`

Input:

```text
呈现器所需的内核依赖项的列表。如果 "NotebookKernel" 中存在任何依赖关系，则可以使用呈现器。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-a21c3874499a1b82

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-a21c3874499a1b82`
- Split: `language_pack`

Input:

```text
{0} 个插入
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-a3dbd60b440641ab

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-a3dbd60b440641ab`
- Split: `language_pack`

Input:

```text
始终以最大化方式打开面板。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-a8e95d30efdb9a64

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-a8e95d30efdb9a64`
- Split: `language_pack`

Input:

```text
“打开预览”操作的图标。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-b0d0ad04694b0ea8

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-b0d0ad04694b0ea8`
- Split: `language_pack`

Input:

```text
未能保存或打开智能体主机分析: {0}
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-b143b64fca5f5204

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-b143b64fca5f5204`
- Split: `language_pack`

Input:

```text
选择下载位置
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-b68b647051b9a314

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-b68b647051b9a314`
- Split: `language_pack`

Input:

```text
{0} 已是最新版本。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-b8a5e8d3e63c3849

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-b8a5e8d3e63c3849`
- Split: `language_pack`

Input:

```text
如果此语法包含嵌入式语言，则为作用域名称到语言 ID 的映射。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-ba8b4ae78b44f3ec

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-ba8b4ae78b44f3ec`
- Split: `language_pack`

Input:

```text
注意: 此属性不适用于 GitHub Copilot 或 Claude 目标。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-c418fc97de5ef87b

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-c418fc97de5ef87b`
- Split: `language_pack`

Input:

```text
添加代码单元格
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-c4afd73241a1bddf

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-c4afd73241a1bddf`
- Split: `language_pack`

Input:

```text
执行使用转发端口的链接
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-c83d1deefd9b34c4

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-c83d1deefd9b34c4`
- Split: `language_pack`

Input:

```text
定义在启用括号对着色时由其嵌套级别着色的括号对。此处包含的任何不包含在{0}中的括号都将自动包含在{0}中。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-c871a62cd669ef44

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-c871a62cd669ef44`
- Split: `language_pack`

Input:

```text
无法单独禁用 "{0}" 扩展。"{1}" 扩展依赖于此扩展。要禁用所有这些扩展吗?
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-ca5513941592c416

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-ca5513941592c416`
- Split: `language_pack`

Input:

```text
活动自定义编辑器差异是否可在内联与并排布局之间切换
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-ca69b4c84786a324

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-ca69b4c84786a324`
- Split: `language_pack`

Input:

```text
模型想要运行所有测试。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-cb126e5b4fa8ec31

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-cb126e5b4fa8ec31`
- Split: `language_pack`

Input:

```text
附加文件，{0}，行 {1} 到行 {2}
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-cc76f89e4c58a0d2

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-cc76f89e4c58a0d2`
- Split: `language_pack`

Input:

```text
添加下一个匹配项(&&N)
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-d31dd4dfff81eca9

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-d31dd4dfff81eca9`
- Split: `language_pack`

Input:

```text
对虚拟工作区如何影响扩展行为及其需要的原因的说明。这仅在 "supported" 不为 "true" 时适用。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-d703bfb0a5c29bea

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-d703bfb0a5c29bea`
- Split: `language_pack`

Input:

```text
放弃更改并重新打开
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-d87fdb7d2e15f8c2

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-d87fdb7d2e15f8c2`
- Split: `language_pack`

Input:

```text
内容从左侧滑入。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-da6ce9d13d0acf5c

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-da6ce9d13d0acf5c`
- Split: `language_pack`

Input:

```text
Web 内容可能包含恶意代码或尝试提示注入攻击。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-e433e1dfab11db9c

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-e433e1dfab11db9c`
- Split: `language_pack`

Input:

```text
切换正则表达式
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-e5872f63c3f3ffdb

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-e5872f63c3f3ffdb`
- Split: `language_pack`

Input:

```text
菜单显示在窗口顶部，并且仅在全屏模式下隐藏。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-e5b17dc9f9497135

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-e5b17dc9f9497135`
- Split: `language_pack`

Input:

```text
图片: {0}
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-e8c009efa14ec353

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-e8c009efa14ec353`
- Split: `language_pack`

Input:

```text
导航路径
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-eca73a68a4ad8491

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-eca73a68a4ad8491`
- Split: `language_pack`

Input:

```text
告诉我 GitHub Copilot 相关信息!
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-ede28d50696fc56b

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-ede28d50696fc56b`
- Split: `language_pack`

Input:

```text
显示稳定版设置
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-eef38cc7593d8aaa

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-eef38cc7593d8aaa`
- Split: `language_pack`

Input:

```text
重置笔记本入门
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-f5510e85cd7fc11b

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-f5510e85cd7fc11b`
- Split: `language_pack`

Input:

```text
与上一个版本比较
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-f684886e6f1776ae

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-f684886e6f1776ae`
- Split: `language_pack`

Input:

```text
“转到下一个”标记的图标。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-f70da209d44d911d

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-f70da209d44d911d`
- Split: `language_pack`

Input:

```text
从不扫描此任务的任务输出
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-f85cf53973975aae

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-f85cf53973975aae`
- Split: `language_pack`

Input:

```text
新的拆分终端将使用父终端开始时使用的工作目录。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-fa06d285423fec57

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-fa06d285423fec57`
- Split: `language_pack`

Input:

```text
控制在编辑器中是否允许通过拖放来移动选中内容。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### vscode-loc-zh-hans-v1/entry-fb67fda66dd60fa8

- Source: `vscode-loc-zh-hans-v1`
- Source case: `entry-fb67fda66dd60fa8`
- Split: `language_pack`

Input:

```text
配置任务(&&C)…
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```
