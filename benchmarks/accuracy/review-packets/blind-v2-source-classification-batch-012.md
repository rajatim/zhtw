<!-- zhtw:disable -->
# Blind-v2 Source Classification 012

Packet: `benchmarks/accuracy/review-packets/blind-v2-source-classification-batch-012.json`
Cases: 100
Seed: `20260719`
Selection: `equal-source-deterministic-sha256-v1`
Selection round: 1

## Rules

- Read only the input and provenance shown in this packet.
- Do not run zhtw, OpenCC, zhconv, Gemini, or another converter.
- Mark `eligible = no` for malformed, unclear, non-Mandarin, or unsuitable text.
- Script: `simplified`, `mixed`, `traditional`, or `uncertain`.
- Domain: `it_api_cli`, `ui_i18n`, `llm_generated`, `formal_news`, `social_daily`, or `high_stakes`.
- Risk: `candidate_gap`, `over_conversion_guard`, or `baseline_guard`.
- Confidence: `high`, `medium`, or `low`; do not guess when context is insufficient.
- This packet is advisory input classification, not expected-output annotation.

## Cases

### ftc-heads-up-simplified-v1/sentence-001

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-001`
- Split: `booklet`

Input:

```text
要在生活中帮助您的孩子安全上网，当心：停一停、想一想、问一问。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-002

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-002`
- Split: `booklet`

Input:

```text
有一些想法可以帮助您与他们开始对话。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-003

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-003`
- Split: `booklet`

Input:

```text
选择一节共同阅读，了解如何谨慎分享、友好在线、防范网络欺凌以及在线保护他们（和自己）的个人信息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-004

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-004`
- Split: `booklet`

Input:

```text
这些工具可以帮助您向孩子们展示如何做出正确的选择、负责任地使用科技。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-006

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-006`
- Split: `booklet`

Input:

```text
上网是生活的一部分。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-007

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-007`
- Split: `booklet`

Input:

```text
人们会在网上观看和创建内容、发布照片和视频、玩游戏、与朋友和家人分享自己所在的位置和所做的事情。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-008

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-008`
- Split: `booklet`

Input:

```text
但在网上发布内容、娱乐和交流时会面临风险。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-009

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-009`
- Split: `booklet`

Input:

```text
网上的某些人和情形并非总是真实可靠的。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-010

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-010`
- Split: `booklet`

Input:

```text
无论手指在键盘、手机或平板电脑上移动得有多快，头脑和时间才是避免网络风险最好的工具。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-011

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-011`
- Split: `booklet`

Input:

```text
停一停，认真想一想面临的情况可以帮助保护你自己、朋友和家人、你的账户和设备。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-012

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-012`
- Split: `booklet`

Input:

```text
否则，最终可能会分享过多信息，让自己或他人尴尬，产生电脑故障，或者与戴着假面具的人交谈。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-013

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-013`
- Split: `booklet`

Input:

```text
想要完全控制谁可以看到你的个人资料、图片、视频或文本是不可能的 — 即使用了在内容被查看后或在 24 小时内将其删除的隐私设置或应用软件也不可能完全控制。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-014

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-014`
- Split: `booklet`

Input:

```text
任何看到你发贴的人都可以截图或录像。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-016

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-016`
- Split: `booklet`

Input:

```text
你分享的事物可能影响到其他人。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-017

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-017`
- Split: `booklet`

Input:

```text
未经当事人准许发送或发布照片和视频可能导致令人尴尬、不公平甚至不安全的状况。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-018

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-018`
- Split: `booklet`

Input:

```text
首先征得当事人的同意。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-020

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-020`
- Split: `booklet`

Input:

```text
如果他们不同意，不要发布。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-021

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-021`
- Split: `booklet`

Input:

```text
网上的行为会产生真实的后果。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-022

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-022`
- Split: `booklet`

Input:

```text
分享的照片、视频和消息会影响你自己、你的隐私、你的声誉和周围的人 — 可能是现在，也可能是将来。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-023

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-023`
- Split: `booklet`

Input:

```text
发贴前停一停，想一想。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-024

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-024`
- Split: `booklet`

Input:

```text
即使你删除了已经发布的内容，或者发布已过期，你不想让人看到的照片或评论也可能被保存、分享或在网上的其他地方存在 — 而且永存。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-025

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-025`
- Split: `booklet`

Input:

```text
发送色情信息：不要这样做。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-026

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-026`
- Split: `booklet`

Input:

```text
你可能在学校或者新闻中听到过人们“发送色情信息” — 用其手机发裸照。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-027

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-027`
- Split: `booklet`

Input:

```text
绝对不能这么做。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-028

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-028`
- Split: `booklet`

Input:

```text
创建、转发甚至保存色情照片、视频或信息会使你面临失去友谊和声誉的风险。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-029

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-029`
- Split: `booklet`

Input:

```text
更糟的是，你的行为还可能违法。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-030

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-030`
- Split: `booklet`

Input:

```text
根据美国卫生部长的说法，使用社交媒体可能会对你造成伤害，具体取决于在社交平台上花费的时间，看到的内容类型，及其对人的健康至关重要的睡眠或锻炼等活动的干扰程度。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-032

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-032`
- Split: `booklet`

Input:

```text
但发短信、发帖、直接消息、玩电子游戏和发电子邮件与面对面的交谈在实施上是一样的。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-033

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-033`
- Split: `booklet`

Input:

```text
请注意自己的沟通方式，务必三思而后行。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-035

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-035`
- Split: `booklet`

Input:

```text
在发送消息前，问自己: “其他人会对这条消息有什么感觉？”
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-036

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-036`
- Split: `booklet`

Input:

```text
考虑并尊重他人在网上的看法和情感 — 就像面对面打交道一样。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-037

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-037`
- Split: `booklet`

Input:

```text
记住：头像和个人资料名称背后都有真人。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-038

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-038`
- Split: `booklet`

Input:

```text
不要全部使用大写字母，也不要用一连串的感叹号或粗大字体。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-040

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-040`
- Split: `booklet`

Input:

```text
不要把所有东西都放在群聊里。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-041

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-041`
- Split: `booklet`

Input:

```text
在群发消息或点击“全部回复”前，停下来想一想：谁需要看到这条消息？
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-042

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-042`
- Split: `booklet`

Input:

```text
冒充他人（例如同学或老师）创建个人资料、评论或发贴是错误的，甚至可能造成伤害。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-043

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-043`
- Split: `booklet`

Input:

```text
如果看到朋友发送了轻率或不安全的内容，提醒他们。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-044

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-044`
- Split: `booklet`

Input:

```text
你可以帮助朋友减少麻烦和难堪。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-045

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-045`
- Split: `booklet`

Input:

```text
如果在网上看到不适宜的内容，报告并告诉可以信赖的成年人。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-046

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-046`
- Split: `booklet`

Input:

```text
大部分应用软件和平台都有报告有危险或不当行为的途径。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-047

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-047`
- Split: `booklet`

Input:

```text
无论是在网上还是实际生活中，每个人都有权利在与他人日常交流时有安全感。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-048

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-048`
- Split: `booklet`

Input:

```text
如果有人发布刻薄的评论、伤人的表情包、尴尬的照片，或者发送关于你的聊天或私人信息，那就是欺凌。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-049

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-049`
- Split: `booklet`

Input:

```text
欺凌是不当的行为。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-050

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-050`
- Split: `booklet`

Input:

```text
向你信赖的成年人讲述这种情况，得到相应的帮助，并决定如何应对。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-051

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-051`
- Split: `booklet`

Input:

```text
不理此人或将其拉黑。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-053

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-053`
- Split: `booklet`

Input:

```text
大部分应用软件和平台都有报告不当或危险行为的工具。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-054

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-054`
- Split: `booklet`

Input:

```text
欺凌往往会让被骚扰的人感觉不好 — 也会让欺凌者形象不佳。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-055

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-055`
- Split: `booklet`

Input:

```text
欺凌还可能导致校方或警方的介入。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-056

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-056`
- Split: `booklet`

Input:

```text
如果你目睹了网络欺凌，想办法加以阻止 — 勇敢出面主持公道。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-057

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-057`
- Split: `booklet`

Input:

```text
如果有人能出面帮助被欺凌的人，恶劣的行为通常很快就会停止。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-058

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-058`
- Split: `booklet`

Input:

```text
网上任何行为都会留下痕迹。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-059

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-059`
- Split: `booklet`

Input:

```text
通过以下步骤确保痕迹不会导致泄露不想分享的信息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-060

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-060`
- Split: `booklet`

Input:

```text
了解如何为设备、应用软件和社交媒体账户开启隐私设置 — 然后加以运用。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-061

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-061`
- Split: `booklet`

Input:

```text
它可帮助限制谁能看到你所在的位置、所发的帖子以及谁能与你联系。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-063

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-063`
- Split: `booklet`

Input:

```text
有些应用软件能让用户看到其朋友所在的位置。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-064

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-064`
- Split: `booklet`

Input:

```text
它们还能分享用户的位置。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-065

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-065`
- Split: `booklet`

Input:

```text
想一想什么时候需要分享自己的位置。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-066

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-066`
- Split: `booklet`

Input:

```text
如果不需要，关闭位置共享。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-067

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-067`
- Split: `booklet`

Input:

```text
设备上的功能（如摄像头）可能有拍照时所在位置的信息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-068

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-068`
- Split: `booklet`

Input:

```text
如果不想每次自拍都让所有人看到自己的位置，请关闭手机摄像头上的位置功能。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-070

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-070`
- Split: `booklet`

Input:

```text
将网友限制为自己生活中认识的人。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-072

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-072`
- Split: `booklet`

Input:

```text
如果不小心，可能会与陌生人分享个人信息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-073

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-073`
- Split: `booklet`

Input:

```text
一旦给了陌生人自己的个人信息 — 例如社保号、密码或者银行账户信息，就没有办法再收回。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-074

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-074`
- Split: `booklet`

Input:

```text
不要回复索要个人信息的邮件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-076

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-076`
- Split: `booklet`

Input:

```text
很可能它们是假冒的，想要窃取个人信息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-077

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-077`
- Split: `booklet`

Input:

```text
向可以信赖的成年人求助，将邮件报告为垃圾邮件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-078

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-078`
- Split: `booklet`

Input:

```text
在下载该应用软件前，检查应用软件想要访问的信息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-079

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-079`
- Split: `booklet`

Input:

```text
有些应用软件申请权限访问它们不需要的信息或功能，例如联系人列表、摄像头、存储、位置和麦克风。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-080

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-080`
- Split: `booklet`

Input:

```text
请可以信赖的成年人帮助阅读应用软件的隐私政策，查看如何使用你的数据，以及是否会分享该数据。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-081

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-081`
- Split: `booklet`

Input:

```text
然后确定那个文字游戏是否真需要访问你的照片。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-082

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-082`
- Split: `booklet`

Input:

```text
在应用软件内购物之前先咨询可以信赖的成年人 — 尤其是在它们自行付费时务必先咨询成年人。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-084

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-084`
- Split: `booklet`

Input:

```text
下面这些步骤可以防止他人侵入你的账户。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-085

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-085`
- Split: `booklet`

Input:

```text
密码越长，越难破解。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-086

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-086`
- Split: `booklet`

Input:

```text
至少有 12 位字符，而且同时含有大小写字母、数字和符号。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-087

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-087`
- Split: `booklet`

Input:

```text
考虑使用随机文字组成的密码短语，让密码不易忘记。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-088

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-088`
- Split: `booklet`

Input:

```text
但不要使用常见的短语、歌词或电影台词，这些都很容易被猜测到。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-089

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-089`
- Split: `booklet`

Input:

```text
使用独特的密码。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-090

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-090`
- Split: `booklet`

Input:

```text
不同的账户使用不同的密码。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-091

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-091`
- Split: `booklet`

Input:

```text
这样，如果有人得到了一个账户的密码，他们无法入侵其他账户。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-092

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-092`
- Split: `booklet`

Input:

```text
可以使用密码管理器来跟踪所有不同的密码。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-094

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-094`
- Split: `booklet`

Input:

```text
精心挑选安全问题。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-095

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-095`
- Split: `booklet`

Input:

```text
尽量选择只有自己能回答的安全问题。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-096

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-096`
- Split: `booklet`

Input:

```text
不要选择其他人可以在网上找到答案的问题 — 例如，自己的邮政编码、出生日期或母亲的闺名。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-097

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-097`
- Split: `booklet`

Input:

```text
如果无法避免这些问题，发挥创意！
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-098

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-098`
- Split: `booklet`

Input:

```text
将它们当成密码，使用随机的长答案。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-099

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-099`
- Split: `booklet`

Input:

```text
但要确保自己能记住答案。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-100

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-100`
- Split: `booklet`

Input:

```text
使用多重身份验证。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-101

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-101`
- Split: `booklet`

Input:

```text
许多账户通过使用“多重身份验证”为账户提供更多保护— 除了密码，还需要其他验证。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-104

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-104`
- Split: `booklet`

Input:

```text
如果公司通知你出现数据泄露，黑客可能得到了你的密码，请迅速更改该账户密码。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-105

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-105`
- Split: `booklet`

Input:

```text
同时更改任何使用类似密码账户的密码。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-107

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-107`
- Split: `booklet`

Input:

```text
确保自己设备的安全。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-108

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-108`
- Split: `booklet`

Input:

```text
将所有设备、互联网浏览器和操作系统的安全软件设置为自动更新。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-110

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-110`
- Split: `booklet`

Input:

```text
不要点击链接或打开附件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-111

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-111`
- Split: `booklet`

Input:

```text
如果在网上收到陌生的短信、电子邮件或消息，要你点击链接或打开附件，切勿听信！
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-112

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-112`
- Split: `booklet`

Input:

```text
即使是免费优惠卷也不要上当。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-113

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-113`
- Split: `booklet`

Input:

```text
链接和附件可能隐藏病毒或间谍软件，它们可能会入侵你的手机、电脑或平板电脑。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-114

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-114`
- Split: `booklet`

Input:

```text
用密码保护设备。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-115

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-115`
- Split: `booklet`

Input:

```text
这将有助于防止照片、消息和帐户落入坏人之手。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-116

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-116`
- Split: `booklet`

Input:

```text
将它们存放在安全的场所。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### ftc-heads-up-simplified-v1/sentence-117

- Source: `ftc-heads-up-simplified-v1`
- Source case: `sentence-117`
- Split: `booklet`

Input:

```text
无论是手机、台式机还是平板电脑，都不要把它留在公共场所 — 即使是只有片刻。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```
