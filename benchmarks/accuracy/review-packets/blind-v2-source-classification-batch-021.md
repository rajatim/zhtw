<!-- zhtw:disable -->
# Blind-v2 Source Classification 021

Packet: `benchmarks/accuracy/review-packets/blind-v2-source-classification-batch-021.json`
Cases: 100
Seed: `20260719`
Selection: `all-source-cases-sorted-v1`

## Rules

- Read only the input and provenance shown in this packet.
- Do not run zhtw, OpenCC, zhconv, Gemini, or another converter.
- Mark `eligible = no` for malformed, unclear, non-Mandarin, or unsuitable text.
- Script: `simplified`, `mixed`, `traditional`, or `uncertain`.
- Domain: `it_api_cli`, `ui_i18n`, `llm_generated`, `formal_news`, `social_daily`, or `high_stakes`.
- Risk: `candidate_gap`, `over_conversion_guard`, or `baseline_guard`.
- Confidence: `high`, `medium`, or `low`; do not guess when context is insufficient.
- This packet is advisory input classification, not expected-output annotation.

## Cases

### zhtw-project-balanced-baseline-guard-v1/formal-001

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-001`
- Split: `project_original`

Input:

```text
委员会将在下周公布会议记录。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-002

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-002`
- Split: `project_original`

Input:

```text
主办单位已完成活动场地的确认工作。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-003

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-003`
- Split: `project_original`

Input:

```text
报告依照章节顺序说明调查过程。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-004

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-004`
- Split: `project_original`

Input:

```text
各部门应在期限内提交执行成果。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-005

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-005`
- Split: `project_original`

Input:

```text
本次会议讨论了年度工作安排。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-006

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-006`
- Split: `project_original`

Input:

```text
承办人员将汇整意见并更新文件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-007

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-007`
- Split: `project_original`

Input:

```text
修订内容自公告之日起开始实施。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-008

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-008`
- Split: `project_original`

Input:

```text
附件列出各项任务的完成日期。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-009

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-009`
- Split: `project_original`

Input:

```text
申请资料不完整者将收到补件通知。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-010

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-010`
- Split: `project_original`

Input:

```text
审核结果预计在月底前寄出。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-011

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-011`
- Split: `project_original`

Input:

```text
负责人已说明计划调整的主要原因。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-012

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-012`
- Split: `project_original`

Input:

```text
新闻稿介绍新服务的适用对象。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-013

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-013`
- Split: `project_original`

Input:

```text
调查结果显示参与人数持续增加。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-014

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-014`
- Split: `project_original`

Input:

```text
工作小组将定期检查办理进度。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-015

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-015`
- Split: `project_original`

Input:

```text
会议结论由秘书处统一整理。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-016

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-016`
- Split: `project_original`

Input:

```text
文件编号 DOC-2026-041 应列在首页右上角。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-017

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-017`
- Split: `project_original`

Input:

```text
附件名称 Appendix C 保留英文格式。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-018

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-018`
- Split: `project_original`

Input:

```text
表格中的项目代码 A-104 不得变更。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-019

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-019`
- Split: `project_original`

Input:

```text
公文引用 ISO 8601 时保留标准编号。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-020

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-020`
- Split: `project_original`

Input:

```text
会议记录中的网址 https://example.org/report 保持不变。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-021

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-021`
- Split: `project_original`

Input:

```text
报告标题沿用“Project North Star”正式名称。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-022

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-022`
- Split: `project_original`

Input:

```text
统计表以 FY2026 表示本财政年度。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-023

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-023`
- Split: `project_original`

Input:

```text
公告中的联系信箱 service@example.org 不应改写。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-024

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-024`
- Split: `project_original`

Input:

```text
决议编号 RES-18/7 必须与附件一致。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/formal-025

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `formal-025`
- Split: `project_original`

Input:

```text
引用原文“as is”时应保留引号内文字。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-001

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-001`
- Split: `project_original`

Input:

```text
系统先读取用户问题，再生成一段简短摘要。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-002

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-002`
- Split: `project_original`

Input:

```text
模型回答前会检查对话上下文是否完整。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-003

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-003`
- Split: `project_original`

Input:

```text
请根据提供的资料整理三个重点。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-004

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-004`
- Split: `project_original`

Input:

```text
回答应使用清楚的段落和完整的句子。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-005

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-005`
- Split: `project_original`

Input:

```text
摘要不得加入原文没有提到的事实。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-006

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-006`
- Split: `project_original`

Input:

```text
模型需要区分用户指令与引用内容。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-007

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-007`
- Split: `project_original`

Input:

```text
检索结果不足时，应明确说明资料有限。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-008

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-008`
- Split: `project_original`

Input:

```text
系统会保留最近十轮对话作为上下文。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-009

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-009`
- Split: `project_original`

Input:

```text
评测人员逐项检查回答是否符合要求。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-010

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-010`
- Split: `project_original`

Input:

```text
生成内容完成后进入人工审核队列。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-011

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-011`
- Split: `project_original`

Input:

```text
提示模板要求模型先列出假设条件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-012

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-012`
- Split: `project_original`

Input:

```text
回答太长时，系统会要求模型重新摘要。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-013

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-013`
- Split: `project_original`

Input:

```text
请把会议记录整理成待办事项。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-014

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-014`
- Split: `project_original`

Input:

```text
分类结果只用于安排后续审核顺序。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-015

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-015`
- Split: `project_original`

Input:

```text
模型应避免重复上一段已经说明的内容。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-016

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-016`
- Split: `project_original`

Input:

```text
批次名称 batch_2026_07 必须保持原样。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-017

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-017`
- Split: `project_original`

Input:

```text
引用中的“human-in-the-loop”不得改写。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-018

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-018`
- Split: `project_original`

Input:

```text
输出对象必须保留字段 review_status。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-019

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-019`
- Split: `project_original`

Input:

```text
系统以 request_id 对应每一次模型请求。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-020

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-020`
- Split: `project_original`

Input:

```text
提示中的 {{source_text}} 是待替换变量。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-021

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-021`
- Split: `project_original`

Input:

```text
模型版本名称 acme-reasoner-v2 不应翻译。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-022

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-022`
- Split: `project_original`

Input:

```text
评分报告保留指标名称 groundedness。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-023

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-023`
- Split: `project_original`

Input:

```text
引用编号 [R-17] 必须对应原始资料。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-024

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-024`
- Split: `project_original`

Input:

```text
系统消息中的 XML 标签 <policy> 必须成对出现。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/llm-025

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `llm-025`
- Split: `project_original`

Input:

```text
失败记录以 status=needs_review 标记。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-001

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-001`
- Split: `project_original`

Input:

```text
大家约好周末一起去看电影。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-002

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-002`
- Split: `project_original`

Input:

```text
我刚刚把照片传到群组里了。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-003

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-003`
- Split: `project_original`

Input:

```text
这家店下午通常不会排太久。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-004

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-004`
- Split: `project_original`

Input:

```text
朋友提醒我出门前记得带雨伞。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-005

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-005`
- Split: `project_original`

Input:

```text
今天的天气比昨天凉快一些。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-006

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-006`
- Split: `project_original`

Input:

```text
我们先在车站出口碰面。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-007

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-007`
- Split: `project_original`

Input:

```text
这段视频我已经看过两次了。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-008

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-008`
- Split: `project_original`

Input:

```text
她在留言区分享了自己的经验。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-009

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-009`
- Split: `project_original`

Input:

```text
晚餐以后大家继续聊天。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-010

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-010`
- Split: `project_original`

Input:

```text
我把明天的行程写在日历上。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-011

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-011`
- Split: `project_original`

Input:

```text
这辆自行车骑起来很轻松。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-012

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-012`
- Split: `project_original`

Input:

```text
请把地址再传一次给我。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-013

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-013`
- Split: `project_original`

Input:

```text
他下班后顺便去取包裹。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-014

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-014`
- Split: `project_original`

Input:

```text
我们改约在室内比较方便。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-015

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-015`
- Split: `project_original`

Input:

```text
这首歌最近常在广播里听到。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-016

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-016`
- Split: `project_original`

Input:

```text
群组名称 Weekend Plan 不需要翻译。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-017

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-017`
- Split: `project_original`

Input:

```text
活动标签 #CityWalk2026 保持原样。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-018

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-018`
- Split: `project_original`

Input:

```text
朋友传来的代码是 RSVP-2048。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-019

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-019`
- Split: `project_original`

Input:

```text
贴文引用“stay curious”这句话。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-020

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-020`
- Split: `project_original`

Input:

```text
照片说明保留地点名称 Green Lake。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-021

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-021`
- Split: `project_original`

Input:

```text
聊天室里的 /remind 指令会建立提醒。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-022

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-022`
- Split: `project_original`

Input:

```text
投票选项 C 保留字母编号。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-023

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-023`
- Split: `project_original`

Input:

```text
分享链接中的 ref=summer 参数不要删除。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-024

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-024`
- Split: `project_original`

Input:

```text
个人简介写着 Product Designer @ Acme。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/social-025

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `social-025`
- Split: `project_original`

Input:

```text
讨论串编号 thread-105 方便之后查找。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-001

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-001`
- Split: `project_original`

Input:

```text
首页会显示最近开启的文件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-002

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-002`
- Split: `project_original`

Input:

```text
用户可以在设置中调整文字大小。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-003

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-003`
- Split: `project_original`

Input:

```text
上传完成后页面会显示成功消息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-004

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-004`
- Split: `project_original`

Input:

```text
搜索结果按照更新时间排列。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-005

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-005`
- Split: `project_original`

Input:

```text
点击按钮即可返回上一个页面。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-006

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-006`
- Split: `project_original`

Input:

```text
通知会在几秒后自动消失。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-007

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-007`
- Split: `project_original`

Input:

```text
列表为空时显示提示文字。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-008

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-008`
- Split: `project_original`

Input:

```text
用户取消操作后关闭对话框。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-009

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-009`
- Split: `project_original`

Input:

```text
文件下载完成时会出现通知。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-010

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-010`
- Split: `project_original`

Input:

```text
切换分页不会清除已经输入的内容。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-011

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-011`
- Split: `project_original`

Input:

```text
系统会记住上次选择的排序方式。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-012

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-012`
- Split: `project_original`

Input:

```text
错误消息应说明无法完成操作的原因。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-013

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-013`
- Split: `project_original`

Input:

```text
侧边栏可以展开或收起。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-014

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-014`
- Split: `project_original`

Input:

```text
表单送出前会检查必填字段。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-015

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-015`
- Split: `project_original`

Input:

```text
页面载入时先显示进度指示。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-016

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-016`
- Split: `project_original`

Input:

```text
按钮文字为“继续使用 {account_name}”。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-017

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-017`
- Split: `project_original`

Input:

```text
对话框标题保留产品名称 Acme Cloud。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-018

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-018`
- Split: `project_original`

Input:

```text
错误代码 ERR_NETWORK_OFFLINE 显示在说明下方。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-019

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-019`
- Split: `project_original`

Input:

```text
分页参数 page_size=50 不随语言改变。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-020

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-020`
- Split: `project_original`

Input:

```text
帮助链接指向 /docs/getting-started。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-021

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-021`
- Split: `project_original`

Input:

```text
快捷键 Ctrl+Alt+K 显示在菜单右侧。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-022

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-022`
- Split: `project_original`

Input:

```text
日期格式 YYYY/MM/DD 由地区设置决定。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-023

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-023`
- Split: `project_original`

Input:

```text
状态值 pending_review 仅供系统内部使用。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-024

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-024`
- Split: `project_original`

Input:

```text
图标的 aria-describedby 属性引用提示文字。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-balanced-baseline-guard-v1/ui-025

- Source: `zhtw-project-balanced-baseline-guard-v1`
- Source case: `ui-025`
- Split: `project_original`

Input:

```text
版本信息显示 build-2026.07.26。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```
