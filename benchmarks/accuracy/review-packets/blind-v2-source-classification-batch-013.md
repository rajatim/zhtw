<!-- zhtw:disable -->
# Blind-v2 Source Classification 013

Packet: `benchmarks/accuracy/review-packets/blind-v2-source-classification-batch-013.json`
Cases: 100
Seed: `20260719`
Selection: `all-source-cases-sorted-v1`

## Rules

- Read only the input and provenance shown in this packet.
- Do not run zhtw, OpenCC, zhconv, Gemini, or another converter.
- Mark `eligible = no` for malformed, unclear, non-Mandarin, or unsuitable text.
- Script: `simplified`, `mixed`, `traditional`, or `uncertain`.
- Domain: `it_api_cli`, `ui_i18n`, `llm_generated`, `formal_news`, `social_daily`, or `high_stakes`.
- Risk: `candidate_gap`, `over_conversion_guard`, or `baseline_guard`.
- Confidence: `high`, `medium`, or `low`; do not guess when context is insufficient.
- This packet is advisory input classification, not expected-output annotation.

## Cases

### zhtw-project-formal-llm-semantic-v1/formal-001

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-001`
- Split: `project_original`

Input:

```text
法院应依照法定程序审理案件，并保障双方陈述意见的机会。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-002

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-002`
- Split: `project_original`

Input:

```text
宪法解释程序启动后，相关机关应在期限内提交意见书。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-003

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-003`
- Split: `project_original`

Input:

```text
委员会通过决议前，应先确认出席人数达到法定门槛。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-004

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-004`
- Split: `project_original`

Input:

```text
法案经表决通过后，仍须依规定完成公布程序。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-005

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-005`
- Split: `project_original`

Input:

```text
公司计划发行公司债，以筹措长期建设所需资金。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-006

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-006`
- Split: `project_original`

Input:

```text
中央银行将发行纪念币，但不会改变现行货币政策。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-007

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-007`
- Split: `project_original`

Input:

```text
出版社预计明年发行修订版，并保留原作者序言。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-008

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-008`
- Split: `project_original`

Input:

```text
主管机关发布调查报告时，应同时公开统计方法。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-009

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-009`
- Split: `project_original`

Input:

```text
市政府发布最新人口统计，并说明资料修订原因。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-010

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-010`
- Split: `project_original`

Input:

```text
博物馆将持续保存文化遗产，并改善典藏环境。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-011

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-011`
- Split: `project_original`

Input:

```text
调查人员必须妥善保存证据，以供后续审理使用。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-012

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-012`
- Split: `project_original`

Input:

```text
档案馆决定保存原始文件，不以扫描副本取代正本。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-013

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-013`
- Split: `project_original`

Input:

```text
研究对象均已签署同意书，并可随时退出研究。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-014

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-014`
- Split: `project_original`

Input:

```text
本项服务的对象包括独居长者与行动不便者。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-015

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-015`
- Split: `project_original`

Input:

```text
多数委员支持这项提案，但要求补充财政影响评估。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-016

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-016`
- Split: `project_original`

Input:

```text
工会表示支持协商结果，并将向会员完整说明。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-017

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-017`
- Split: `project_original`

Input:

```text
法院裁定停止执行原处分，直到诉讼结果确定。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-018

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-018`
- Split: `project_original`

Input:

```text
承办机关应确实执行决议，不得自行增加限制条件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-019

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-019`
- Split: `project_original`

Input:

```text
图表中的水平线代表基准值，垂直线代表观察日期。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-020

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-020`
- Split: `project_original`

Input:

```text
实验结果符合质量守恒定律，误差也在容许范围内。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-021

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-021`
- Split: `project_original`

Input:

```text
研究团队重新测量样本质量，以排除仪器校准误差。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-022

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-022`
- Split: `project_original`

Input:

```text
修复人员保留了建筑原有窗口，没有改成封闭墙面。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-023

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-023`
- Split: `project_original`

Input:

```text
代表团结束访问后，将发布双方共同声明。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-024

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-024`
- Split: `project_original`

Input:

```text
记者访问多位居民，确认事件发生时的现场情况。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-025

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-025`
- Split: `project_original`

Input:

```text
公司完成清算后，才能申请注销登记。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-026

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-026`
- Split: `project_original`

Input:

```text
主管机关不得以注销账号代替注销公司登记。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-027

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-027`
- Split: `project_original`

Input:

```text
这项研究应用既有理论解释新发现，没有改变原始定义。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-028

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-028`
- Split: `project_original`

Input:

```text
铁路终端车站将在施工期间维持部分月台开放。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-029

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-029`
- Split: `project_original`

Input:

```text
偏远地区需要长期医疗支持，不能只依赖短期巡回服务。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-030

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-030`
- Split: `project_original`

Input:

```text
消息人士拒绝透露姓名，但提供了可独立核实的文件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-031

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-031`
- Split: `project_original`

Input:

```text
消费者可以在法定期间内解除契约并请求退款。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-032

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-032`
- Split: `project_original`

Input:

```text
比赛项目分为个人组与团体组，计分方式各不相同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-033

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-033`
- Split: `project_original`

Input:

```text
统计模型包含三个变量，并分别报告估计值与信赖区间。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-034

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-034`
- Split: `project_original`

Input:

```text
该函数在定义域内连续，但不保证处处可微分。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-035

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-035`
- Split: `project_original`

Input:

```text
服务对象的需求不同，补助标准不宜一律相同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-036

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-036`
- Split: `project_original`

Input:

```text
电视节目将完整播出访谈，不剪除受访者的重要说明。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-037

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-037`
- Split: `project_original`

Input:

```text
指挥官部署救援人员时，应保留紧急撤离通道。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-038

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-038`
- Split: `project_original`

Input:

```text
港口发现受损容器后，立即划设安全区域。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-039

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-039`
- Split: `project_original`

Input:

```text
镜像中的文字左右相反，这是光学反射造成的现象。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-040

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-040`
- Split: `project_original`

Input:

```text
地方分支机构可以受理申请，但无权变更审查标准。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-041

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-041`
- Split: `project_original`

Input:

```text
工厂将更新流水线，同时保留熟练员工的操作岗位。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-042

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-042`
- Split: `project_original`

Input:

```text
演员在首场演出的表现稳定，获得多数评论肯定。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-043

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-043`
- Split: `project_original`

Input:

```text
车站统计尖峰时段旅客流量，作为调整班次的依据。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-044

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-044`
- Split: `project_original`

Input:

```text
转运中心是区域交通节点，连接铁路与公路运输。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-045

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-045`
- Split: `project_original`

Input:

```text
船长在航海日志中记录天气、航向与设备状况。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-046

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-046`
- Split: `project_original`

Input:

```text
主管机关决定撤销原处分，并重新进行调查。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-047

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-047`
- Split: `project_original`

Input:

```text
审议结果返回委员会后，还需安排第二次表决。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-048

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-048`
- Split: `project_original`

Input:

```text
专家建议扩展保护范围，但不要改变法律定义。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-049

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-049`
- Split: `project_original`

Input:

```text
搜救队沿既定路径前进，并在每个节点回报位置。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/formal-050

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `formal-050`
- Split: `project_original`

Input:

```text
大会发布命令后，各单位依原定程序执行疏散。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-001

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-001`
- Split: `project_original`

Input:

```text
模型摘要法律文件时，必须保留程序与程式之间的语义差异。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-002

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-002`
- Split: `project_original`

Input:

```text
模型应先判断文件是正式文书还是计算机文件，再选择适当用词。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-003

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-003`
- Split: `project_original`

Input:

```text
回答不得把发行股票写成发布股票，也不能混淆公告发布。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-004

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-004`
- Split: `project_original`

Input:

```text
翻译内容提到保存古迹时，不应套用保存计算机资料的说法。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-005

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-005`
- Split: `project_original`

Input:

```text
模型需要区分研究对象、结婚对象与程序设计对象。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-006

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-006`
- Split: `project_original`

Input:

```text
物理问题中的质量表示物理量，不是产品品质。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-007

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-007`
- Split: `project_original`

Input:

```text
几何题中的水平是方向，不是能力或发展程度。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-008

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-008`
- Split: `project_original`

Input:

```text
图片描述中的窗口是建筑构件，不一定是软件界面。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-009

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-009`
- Split: `project_original`

Input:

```text
餐厅菜单与软件导航菜单可能需要不同的本地化处理。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-010

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-010`
- Split: `project_original`

Input:

```text
外交访问与系统访问属于不同语境，模型不得混为一谈。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-011

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-011`
- Split: `project_original`

Input:

```text
地址可能指邮寄地点，也可能出现在网络配置说明中。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-012

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-012`
- Split: `project_original`

Input:

```text
远程山区与远程连接的语境不同，摘要时应保留原意。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-013

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-013`
- Split: `project_original`

Input:

```text
新闻中的消息人士不是系统中的消息消费者。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-014

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-014`
- Split: `project_original`

Input:

```text
消费者保护文章中的消费者是一般民众，不是队列处理程序。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-015

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-015`
- Split: `project_original`

Input:

```text
行政事务与数据库事务不能因为写法相同就视为同一概念。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-016

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-016`
- Split: `project_original`

Input:

```text
教材中的实例是说明案例，云端文件中的实例则可能指运算资源。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-017

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-017`
- Split: `project_original`

Input:

```text
光学镜像与容器镜像的定义不同，回答时应说明所属领域。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-018

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-018`
- Split: `project_original`

Input:

```text
军事部署与软件部署使用相同词形，但执行对象并不相同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-019

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-019`
- Split: `project_original`

Input:

```text
组织分支与代码分支都可简称分支，模型必须依上下文判断。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-020

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-020`
- Split: `project_original`

Input:

```text
铁路终端与命令行终端不是同一种设施或工具。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-021

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-021`
- Split: `project_original`

Input:

```text
港口新闻中的端口可能只是原文误用，模型应先确认语境。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-022

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-022`
- Split: `project_original`

Input:

```text
会议主持人与计算机主机没有相同含义，模型不应机械对应。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-023

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-023`
- Split: `project_original`

Input:

```text
餐厅服务人员与网络服务器都提供服务，但指涉对象不同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-024

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-024`
- Split: `project_original`

Input:

```text
公共图书馆与软件程序库都可能被简称为库，回答应避免歧义。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-025

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-025`
- Split: `project_original`

Input:

```text
快递包裹与软件包属于不同领域，翻译时不能只看单字。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-026

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-026`
- Split: `project_original`

Input:

```text
产品类别与程序设计类都涉及分类，但术语用法并不完全相同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-027

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-027`
- Split: `project_original`

Input:

```text
原文中的同一个词可能指农田，也可能指资料字段，模型应参考整句。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-028

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-028`
- Split: `project_original`

Input:

```text
数学函数与程序函数应分别采用所属领域的惯用名称。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-029

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-029`
- Split: `project_original`

Input:

```text
数学变量与模板变量可以同名，但不得擅自修改变量内容。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-030

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-030`
- Split: `project_original`

Input:

```text
执行法院判决与执行自动化任务的法律效果完全不同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-031

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-031`
- Split: `project_original`

Input:

```text
撤销行政处分不等于复原界面上的最后一步操作。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-032

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-032`
- Split: `project_original`

Input:

```text
经济恢复与备份恢复的对象不同，摘要不能省略关键名词。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-033

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-033`
- Split: `project_original`

Input:

```text
应用科学理论与开启移动应用程序属于不同动作。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-034

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-034`
- Split: `project_original`

Input:

```text
扩展保护范围与安装浏览器扩展功能不可混写。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-035

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-035`
- Split: `project_original`

Input:

```text
装载货物与加载网页虽然都涉及载入，但语境不同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-036

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-036`
- Split: `project_original`

Input:

```text
链条的连接处与网页链接不一定采用相同表达。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-037

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-037`
- Split: `project_original`

Input:

```text
商品标签与界面分页标签应根据实际功能分别描述。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-038

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-038`
- Split: `project_original`

Input:

```text
登山路径与文件路径都表示路线，但使用场景并不相同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-039

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-039`
- Split: `project_original`

Input:

```text
道路流量与网络流量的单位不同，模型不得合并数值。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-040

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-040`
- Split: `project_original`

Input:

```text
交通节点与运算节点可能出现在同一份智慧城市报告中。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-041

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-041`
- Split: `project_original`

Input:

```text
舞台表现与系统性能都可评价好坏，但指标并不相同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-042

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-042`
- Split: `project_original`

Input:

```text
记者回应与接口响应的主体不同，生成摘要时应清楚标示。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-043

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-043`
- Split: `project_original`

Input:

```text
个人日记与系统日志都记录事件，但隐私要求不同。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-044

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-044`
- Split: `project_original`

Input:

```text
统计指标与监控指标可以共用名称，但计算方式必须分开说明。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-045

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-045`
- Split: `project_original`

Input:

```text
追踪包裹与分布式追踪使用不同技术，不应共用操作步骤。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-046

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-046`
- Split: `project_original`

Input:

```text
人的记忆与计算机内存不是同一个概念，类比时要注明限制。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-047

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-047`
- Split: `project_original`

Input:

```text
模型引用专有名词时，应保持官方名称，不要自行套用地域词。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-048

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-048`
- Split: `project_original`

Input:

```text
引文中的术语必须忠实保留，解释文字才可依目标读者调整。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-049

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-049`
- Split: `project_original`

Input:

```text
一句话同时出现多个可能含义时，模型应指出歧义而不是猜测。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-formal-llm-semantic-v1/llm-050

- Source: `zhtw-project-formal-llm-semantic-v1`
- Source case: `llm-050`
- Split: `project_original`

Input:

```text
上下文不足以判断术语含义时，应请求补充信息再继续处理。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```
