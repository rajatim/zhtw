<!-- zhtw:disable -->
# Blind-v2 Source Classification 031

Packet: `benchmarks/accuracy/review-packets/blind-v2-source-classification-batch-031.json`
Cases: 100
Seed: `20260719`
Selection: `all-source-cases-sorted-v1`

## Rules

- Read only the input and provenance shown in this packet.
- Do not run zhtw, OpenCC, zhconv, Gemini, or another converter.
- Mark `eligible = no` for malformed, unclear, non-Mandarin, or unsuitable text.
- Script: `simplified`, `mixed`, `traditional`, or `uncertain`.
- Domain: `it_api_cli`, `ui_i18n`, `llm_generated`, `formal_news`, `social_daily`, or `high_stakes`.
- Risk: `candidate_gap`, `over_conversion_guard`, or `baseline_guard`.
- Confidence: `high`, `medium`, or `low`; do not guess when context is insufficient.
- This packet is advisory input classification, not expected-output annotation.

## Cases

### zhtw-project-llm-domain-balance-v1/formal-001

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-001`
- Split: `project_original`

Input:

```text
委员会将在完成调查后公布书面报告。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-002

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-002`
- Split: `project_original`

Input:

```text
主管机关要求业者在期限内补正资料。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-003

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-003`
- Split: `project_original`

Input:

```text
审计人员逐项核对付款日期与支出凭证。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-004

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-004`
- Split: `project_original`

Input:

```text
申请人应在表格中填写有效的联系方式。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-005

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-005`
- Split: `project_original`

Input:

```text
地方政府已说明预算执行进度与差异原因。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-006

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-006`
- Split: `project_original`

Input:

```text
会议记录须载明出席人员和表决结果。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-007

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-007`
- Split: `project_original`

Input:

```text
本办法自公告之日起正式施行。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-008

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-008`
- Split: `project_original`

Input:

```text
承办单位将汇总意见并修订实施计划。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-009

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-009`
- Split: `project_original`

Input:

```text
新闻稿引用的统计数字以原始资料为准。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-010

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-010`
- Split: `project_original`

Input:

```text
法院文件沿用案件编号 115年度诉字第42号。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-011

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-011`
- Split: `project_original`

Input:

```text
契约附件应与采购公告的版本一致。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-012

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-012`
- Split: `project_original`

Input:

```text
银行将在交易完成后寄送电子对账单。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-013

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-013`
- Split: `project_original`

Input:

```text
保险公司应向客户说明保单的重要条款。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-014

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-014`
- Split: `project_original`

Input:

```text
医疗机构必须妥善保存患者的检查记录。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-015

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-015`
- Split: `project_original`

Input:

```text
研究报告以 DOI 10.1234/example.2026.7 标识论文。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-016

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-016`
- Split: `project_original`

Input:

```text
决议附件保留文件代号 A-17，不改写编号。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-017

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-017`
- Split: `project_original`

Input:

```text
财政年度 FY2027 的预算金额另表列示。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-018

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-018`
- Split: `project_original`

Input:

```text
国际公报中的 Côte d’Ivoire 应维持官方拼写。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-019

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-019`
- Split: `project_original`

Input:

```text
检验报告须注明仪器型号和校准日期。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/formal-020

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `formal-020`
- Split: `project_original`

Input:

```text
个人资料仅能用于原先告知的特定目的。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-001

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-001`
- Split: `project_original`

Input:

```text
系统提示词要求模型先确认用户的问题范围。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-002

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-002`
- Split: `project_original`

Input:

```text
工具调用失败后，助理应说明错误原因。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-003

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-003`
- Split: `project_original`

Input:

```text
检索流程会从多个文档中挑选相关片段。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-004

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-004`
- Split: `project_original`

Input:

```text
请将结果输出为有效的 JSON 对象。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-005

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-005`
- Split: `project_original`

Input:

```text
模型需要在上下文窗口内保留关键资料。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-006

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-006`
- Split: `project_original`

Input:

```text
用户意图不明确时，应先提出澄清问题。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-007

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-007`
- Split: `project_original`

Input:

```text
向量嵌入用于计算查询与段落的相似度。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-008

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-008`
- Split: `project_original`

Input:

```text
重排序模型会调整搜索结果的优先级。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-009

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-009`
- Split: `project_original`

Input:

```text
回答不得虚构来源中没有出现的数字。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-010

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-010`
- Split: `project_original`

Input:

```text
每项结论都必须附上对应的引用编号。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-011

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-011`
- Split: `project_original`

Input:

```text
模板中的 {{user_query}} 是待替换的占位符。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-012

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-012`
- Split: `project_original`

Input:

```text
请求标头中的 API_TOKEN 不得写入日志。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-013

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-013`
- Split: `project_original`

Input:

```text
请保留 Markdown 表格内的代码片段。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-014

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-014`
- Split: `project_original`

Input:

```text
YAML 文件中的缩进层级不能改变。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-015

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-015`
- Split: `project_original`

Input:

```text
生成的 SQL 查询只能读取审计视图。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-016

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-016`
- Split: `project_original`

Input:

```text
函数名称 fetch_user_profile 必须保持原样。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-017

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-017`
- Split: `project_original`

Input:

```text
错误代码 E_MODEL_TIMEOUT 表示模型响应超时。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-018

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-018`
- Split: `project_original`

Input:

```text
将 temperature 设为 0 可以减少随机变化。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-019

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-019`
- Split: `project_original`

Input:

```text
参数 top_p 的数值范围是 0 到 1。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-020

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-020`
- Split: `project_original`

Input:

```text
模型名称 acme-reasoner-v3 不应翻译。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-021

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-021`
- Split: `project_original`

Input:

```text
评测脚本会比较模型输出与参考答案。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-022

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-022`
- Split: `project_original`

Input:

```text
训练数据集不得包含真实客户的个人资料。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-023

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-023`
- Split: `project_original`

Input:

```text
验证集只用于选择模型参数，不参与训练。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-024

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-024`
- Split: `project_original`

Input:

```text
这个批次包含一百条待审核的测试样本。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-025

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-025`
- Split: `project_original`

Input:

```text
系统会从最近的检查点恢复训练任务。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-026

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-026`
- Split: `project_original`

Input:

```text
向量数据库保存文档片段和来源标识。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-027

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-027`
- Split: `project_original`

Input:

```text
缓存过期后，服务会重新计算嵌入结果。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-028

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-028`
- Split: `project_original`

Input:

```text
任务队列按照请求的优先级依次处理。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-029

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-029`
- Split: `project_original`

Input:

```text
流式输出会逐段显示模型生成的内容。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-030

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-030`
- Split: `project_original`

Input:

```text
请求失败时最多重试三次，并记录状态码。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-031

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-031`
- Split: `project_original`

Input:

```text
低置信度回答必须进入人工审核队列。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-032

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-032`
- Split: `project_original`

Input:

```text
分类器的置信度阈值由管理员设定。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-033

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-033`
- Split: `project_original`

Input:

```text
对话记录应依照隐私政策定期删除。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-034

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-034`
- Split: `project_original`

Input:

```text
系统会遮盖姓名、电话和身份证号等敏感资料。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-035

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-035`
- Split: `project_original`

Input:

```text
模型回答前必须检查适用的内容政策。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-036

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-036`
- Split: `project_original`

Input:

```text
安全过滤器会拦截包含恶意指令的请求。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-037

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-037`
- Split: `project_original`

Input:

```text
消息中的 role 字段只能是 user 或 assistant。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-038

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-038`
- Split: `project_original`

Input:

```text
多轮对话需要区分当前问题和历史消息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-039

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-039`
- Split: `project_original`

Input:

```text
长期记忆只保存用户明确同意的偏好。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-040

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-040`
- Split: `project_original`

Input:

```text
摘要应保留决定、负责人和完成期限。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-041

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-041`
- Split: `project_original`

Input:

```text
请使用台湾繁体中文回答后续问题。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-042

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-042`
- Split: `project_original`

Input:

```text
引文中的“chain of thought”不得意译。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-043

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-043`
- Split: `project_original`

Input:

```text
实体名称 OpenAI Responses API 必须完整保留。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-044

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-044`
- Split: `project_original`

Input:

```text
时间戳 2026-07-27T16:45:00+08:00 不得改写。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-045

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-045`
- Split: `project_original`

Input:

```text
来源网址 https://example.com/report?id=42 必须保持有效。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-046

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-046`
- Split: `project_original`

Input:

```text
附件名称 quarterly_review_v2.pdf 不应转换。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-047

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-047`
- Split: `project_original`

Input:

```text
代码块里的变量名和字符串常量必须保留。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-048

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-048`
- Split: `project_original`

Input:

```text
正则表达式 ^user_[0-9]+$ 用于检查账号格式。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-049

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-049`
- Split: `project_original`

Input:

```text
比较表应列出每个模型的延迟和错误率。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/llm-050

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `llm-050`
- Split: `project_original`

Input:

```text
无法安全回答时，助理应简短说明拒绝原因。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-001

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-001`
- Split: `project_original`

Input:

```text
请提醒我下班后去超市买牛奶。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-002

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-002`
- Split: `project_original`

Input:

```text
明天早上六点半帮我设置闹钟。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-003

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-003`
- Split: `project_original`

Input:

```text
附近有没有适合家庭聚餐的餐厅？
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-004

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-004`
- Split: `project_original`

Input:

```text
把这张照片发送到朋友群组。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-005

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-005`
- Split: `project_original`

Input:

```text
周末的电影票已经全部卖完了。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-006

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-006`
- Split: `project_original`

Input:

```text
我的手机电量只剩百分之十。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-007

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-007`
- Split: `project_original`

Input:

```text
导航建议从下一个路口右转。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-008

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-008`
- Split: `project_original`

Input:

```text
外卖订单预计四十分钟后送达。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-009

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-009`
- Split: `project_original`

Input:

```text
请把购物清单里的土豆删除。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-010

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-010`
- Split: `project_original`

Input:

```text
我想取消今晚八点的预约。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-011

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-011`
- Split: `project_original`

Input:

```text
王小明刚才发来一条语音消息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-012

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-012`
- Split: `project_original`

Input:

```text
台北今天下午可能会下大雨。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-013

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-013`
- Split: `project_original`

Input:

```text
这份食谱需要两百克面粉。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-014

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-014`
- Split: `project_original`

Input:

```text
请播放陈绮贞的《旅行的意义》。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/social-015

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `social-015`
- Split: `project_original`

Input:

```text
我已经把会议地址传到群组了。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-001

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-001`
- Split: `project_original`

Input:

```text
点击按钮即可上传文件。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-002

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-002`
- Split: `project_original`

Input:

```text
搜索框的占位文字为“输入关键词”。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-003

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-003`
- Split: `project_original`

Input:

```text
当前账号没有访问此页面的权限。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-004

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-004`
- Split: `project_original`

Input:

```text
启用通知后即可接收最新消息。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-005

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-005`
- Split: `project_original`

Input:

```text
下载完成后，请重新启动应用程序。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-006

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-006`
- Split: `project_original`

Input:

```text
将鼠标指针移到图标上可查看提示。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-007

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-007`
- Split: `project_original`

Input:

```text
此操作会永久删除所选文件夹。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-008

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-008`
- Split: `project_original`

Input:

```text
系统正在同步您的联系人和日历。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-009

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-009`
- Split: `project_original`

Input:

```text
拖动滑块可以调整屏幕亮度。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-010

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-010`
- Split: `project_original`

Input:

```text
请从下拉菜单中选择语言。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-011

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-011`
- Split: `project_original`

Input:

```text
验证码将在十分钟后失效。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-012

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-012`
- Split: `project_original`

Input:

```text
按 Ctrl+Shift+P 打开命令面板。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-013

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-013`
- Split: `project_original`

Input:

```text
错误代码 ERR_FILE_LOCKED 表示文件已被占用。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-014

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-014`
- Split: `project_original`

Input:

```text
版本号 v3.2.1 显示在关于页面。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```

### zhtw-project-llm-domain-balance-v1/ui-015

- Source: `zhtw-project-llm-domain-balance-v1`
- Source case: `ui-015`
- Split: `project_original`

Input:

```text
请勿修改路径 /Users/demo/Documents。
```

Classification:

```text
eligible:
script:
domain:
risk:
quality_flags:
confidence:
notes:
```
